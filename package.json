{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "bower":"bower",
    "psci":"pulp psci",
    "test": "pulp test"
  },
  "dependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1",
    "bower":"^1.8.0"
  }
}
